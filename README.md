<!-- run docker in interactive mode -->

docker compose exec backend sh

TODOs:

- [] we'll implement AI queries later on. User types a natural language query, AI writes in SQL query, we perform checks and safety stuff. if it's okay to run then we run it and show the results to users. else warn the user.
